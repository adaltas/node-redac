{
  "name": "redac",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/config-lerna-scopes": "^17.6.6",
    "husky": "^8.0.3",
    "lerna": "^6.6.2"
  },
  "scripts": {
    "postinstall": "husky install",
    "test": "lerna run test",
    "version": "lerna version"
  }
}
